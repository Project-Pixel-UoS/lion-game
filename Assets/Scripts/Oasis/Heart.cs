using UnityEngine;
using UnityEngine.UI;

// Sits on the heart prefab. Exposes the fill image so the
// hearts display can tint it without touching the outline.
public class Heart : MonoBehaviour
{
    public Image fill;
}
